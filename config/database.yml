# ============================================
# Database Configuration (Environment-driven)
# ============================================

default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>
  username: <%= ENV.fetch("DB_USERNAME") %>
  password: <%= ENV.fetch("DB_PASSWORD") %>
  host: <%= ENV.fetch("DB_HOST") %>
  port: <%= ENV.fetch("DB_PORT") %>
  database: <%= ENV.fetch("DB_NAME") %>

development:
  <<: *default

test:
  <<: *default

production:
  primary:
    <<: *default
  cable:
    <<: *default
    database: studyroom_service_cable_production
  cache:
    <<: *default
    database: studyroom_service_cache_production
  queue:
    <<: *default
    database: studyroom_service_queue_production